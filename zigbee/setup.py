#!/usr/bin/env python

from setuptools import setup

setup(name='xbeelib',
      version='v0.1',
      description='xbee tx lib',
      packages=["xbeelib"],
      entry_points={'console_scripts': [] },
      include_package_data=True,
      zip_safe=True
      )


